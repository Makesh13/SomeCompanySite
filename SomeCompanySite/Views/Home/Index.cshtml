@model TextField
@{
    ViewBag.Title = Model.MetaTitle;
    ViewBag.Description = Model.MetaDescription;
    ViewBag.Keywords = Model.MetaKeywords;
}
<div>
    @Html.Raw(Model.Text)@*Метод Raw выводит текст в том виде как он есть(тоесть все теги сохраняются)*@
    @*С методом Raw нужно быть осторожным, по скольку, если в содержимом, которое выводится этим
        методом, например, будет жыс скрипт, то он выполнится. Надо на стороне сервера проверять содержимое*@
</div>